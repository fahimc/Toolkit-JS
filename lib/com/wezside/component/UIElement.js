var UIElement = function() {
};
(function() {
	/*
	 * local variables
	 */
	var _ = Class.extend(UIElement, DisplayObject);
	/*
	 * public functions
	 */
	_.build = function() {
		/*
		 * build code
		 */
		Class._super(this, "init");
	}
})();
